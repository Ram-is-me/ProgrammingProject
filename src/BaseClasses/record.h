#ifndef RECORD_H
#define RECORD_H
#include <vector>
#include <string>
using namespace std;
class Record
{
    public:
    private:
    vector<string> record; //Record with String
    vector<double> numRow; //Numerical Record
    int dim; //Dimension of Record
};

#endif